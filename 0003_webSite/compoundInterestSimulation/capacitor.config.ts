import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'compoundinterestsimulation',
  webDir: 'build'
};

export default config;
